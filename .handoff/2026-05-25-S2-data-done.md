# S2 Data Done — 5 黄金数据文件就绪

**From**: Data Agent
**To**: Tools Agent / Engine Agent / Spec Owner (林品臣)
**Tag**: `s2-data-done`

## 交付

| 文件 | 条目 | DSP 钩子 |
|------|------|----------|
| `src/data/resumes.json` | 30 条 | DSP-2(三人对比), DSP-3(张三 Memory), DSP-4(张三面试) |
| `src/data/jobs.json` | 10 条 | DSP-1(不含 BSP) |
| `src/data/pipeline.json` | 10 个岗位 | DSP-5(算法岗 28%→19%) |
| `src/data/market.json` | 15 个方向 | DSP-1(含 BSP/嵌入式市场数据) |
| `src/data/salary.json` | 12 个岗位 | DSP-1(含 BSP 薪酬对标) |
| `src/data/README.md` | 文档 | 5 条 DSP 故事点索引 |
| `src/data/index.ts` | 适配层 | `_meta` + `data` 解包，新增 meta 导出函数 |
| `scripts/validate-data.ts` | 校验脚本 | 24 项校验全绿 |

## 关键决策

1. **res_007 张三重写**: 原 res_007 周睿被替换为张三，含完整 careerHistory + projects + notes 预埋
2. **数据结构调整**: 数组文件 (resumes/jobs) 变为 `{_meta, data: [...]}`, 对象文件 (pipeline/market/salary) 新增 `_meta` 同级 key
3. **DSP 钩子可反查**: 每个文件的 `_meta.dsp_hooks` 明确标注 DSP 编号和故事点位置
4. **同义词扩展**: `index.ts` 新增 `bsp` 同义词组，确保 "嵌入式/驱动/固件/底层" 等词能被搜索命中

## 验证

```
npx tsx scripts/validate-data.ts
✅ Resumes: 6 项通过
✅ Jobs: 5 项通过
✅ Pipeline: 4 项通过
✅ Market: 4 项通过
✅ Salary: 3 项通过
✅ README.md: 存在
```

## 已知 TODO

- T9/T10 (Memory) 仍为 stub，S5 才实现。张三的 notes 数据已预埋，S5 可直接复用
- `tsc --noEmit` 未运行，待 Tools Agent 联调时一起验证
- 联调 integration test 待 Tools Agent 完成 12 工具后一起跑

## 解锁

- **Tools Agent** 可基于这些数据实现 12 个真实工具
- **Engine Agent** 可替换 mock tool 为真实 tool
- **UI Agent** 可基于真实数据调试 C1-C10
- **Eval Agent** 可基于 DSP 钩子编排验收测试
