# S1 Done — Engine Loop Ready

**From**: Engine Agent
**To**: Tools Agent / UI Agent / Spec Owner
**日期**: 2026-05-25

## 交付

- `AIEngine` 已实现，Function Calling Loop + 四件套止损全部跑通
- 7 个单测全绿 (`npx tsx test/engine.test.ts`)，覆盖正常/止损/降级三类路径
- 事件 schema 严格按 `contracts/events.ts` emit，S7 可消费
- `src/engine/__mocks__/` 包含 MockToolExecutor 和 MockLLMClient，可直接导入复用
- `tsc --noEmit` 零报错

## 解锁

- **Tools Agent**：可基于 `ToolExecutor` 接口把 mock 换成真实实现（S2）
- **UI Agent**：可基于 `ChatOutput` 接卡片渲染（S3）。注意：ChatView.tsx 当前通过向后兼容桩运行，S3 需迁移到新 API:
  ```ts
  // 旧 API（S3 需替换）
  new AIEngine(role, apiKey, baseUrl, model)
  engine.processInput(msg)
  // 新 API
  new AIEngine({ llmClient, toolExecutor })
  engine.chat({ role: '用人经理', message: msg, sessionId })
  ```
- **Eval Agent**：事件流已就绪（S4）

## 文件清单

| 文件 | 动作 | 行数 |
|------|------|------|
| `src/engine/AIEngine.ts` | 重写 | 499 |
| `src/engine/stop_guards.ts` | 新建 | 72 |
| `src/engine/events.ts` | 新建 | 182 |
| `src/engine/__mocks__/mockTools.ts` | 新建 | 206 |
| `src/engine/__mocks__/mockLLM.ts` | 新建 | 97 |
| `src/engine/index.ts` | 修改 | 45 |
| `test/engine.test.ts` | 新建 | 261 |
| `docs/slices/S1-engine.md` | 新建 | 49 |
| `STATE.md` | 修改 | 1 行 |

## 已知 TODO

- prompt 工程未优化（baseline 跑通即可）
- `ChatInput.memory` 字段为 `unknown`，S5 再定接口
- `ChatOutput.cards` 当前为空数组 `[]`，S3 填卡片
- ChatView.tsx 旧 API 兼容桩中的 `processInput()` 和旧构造器会 throw 迁移提示

## 风险/请 Spec Owner 确认

- `ChatInput.role` 对外用中文（`'用人经理' | '招聘HR' | '候选人'`），与旧版 `'hm' | 'hr' | 'candidate'` 不同。UI 层迁移时需注意
